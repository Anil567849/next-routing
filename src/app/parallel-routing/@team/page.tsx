import React from 'react'

async function Team() {
    // await new Promise((resolve, reject) => {
    //     setTimeout(() => {
    //         resolve(0);
    //     }, 5000);
    // })

    throw new Error("hello");
    
  return (
    <div>Team</div>
  )
}

export default Team